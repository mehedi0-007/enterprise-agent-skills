# Secret Lifecycle

Create → store → provision → use → audit → rotate → revoke → remove.

At each stage ask:
- who can access it?
- can access be reduced?
- can a shorter-lived credential be used?
- where is the credential copied?
- how will compromise be detected?
