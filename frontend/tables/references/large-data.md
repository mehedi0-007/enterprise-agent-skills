# Large Data

For large datasets:
- server-side filtering/sorting
- bounded pagination/cursors
- selective projections
- appropriate indexes
- measured query path
- measured browser rendering

Virtualization is for DOM/rendering scale, not a substitute for server-side bounds.
