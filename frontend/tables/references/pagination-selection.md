# Pagination and Selection

Define whether selection is:
- current page only
- persisted across pages
- all filtered results

If "select all" means all matching records, communicate that scope and provide a way to review/change it.
