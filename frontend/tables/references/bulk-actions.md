# Bulk Actions

For a bulk operation:
1. show selected scope
2. enforce per-item authorization server-side
3. define atomic vs partial success
4. show progress for long work
5. return succeeded/failed counts
6. offer retry for failed items when safe
7. confirm destructive/high-impact operations
