# Table Accessibility

Prefer semantic table markup for tabular information.

For sortable columns:
- header has accessible name
- current sort direction is exposed

For selection:
- checkbox has clear label/scope
- selection state is perceivable

For interactive rows:
- use real links/buttons
- keep keyboard order logical

Do not introduce ARIA grid complexity unless true grid behavior is required.
