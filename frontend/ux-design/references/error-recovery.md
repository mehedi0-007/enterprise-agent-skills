# Error Recovery

Match error to scope:

Validation → fix field
Permission → request access/change context
Conflict → reload/merge
Timeout/network → retry
Server failure → preserve work + retry/support
Not found → navigate/recover

A message without a recovery path is often incomplete.
