# Permission-Aware Navigation

Visibility can improve UX but is never authorization.

When access is removed:
- update navigation
- protect direct route/API
- avoid stale cached permission exposure
- handle currently open protected pages

For sensitive resources, align UI behavior with backend 403/404 semantics.
