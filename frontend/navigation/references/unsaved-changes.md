# Unsaved Changes

Warn only when leaving would lose meaningful work.

Define:
- dirty condition
- save/saving/saved/error state
- back/navigation behavior
- recovery

For autosave, don't warn merely because a form is open if data is durably saved.
