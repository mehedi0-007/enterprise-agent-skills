# Native Semantics First

Prefer:
button over clickable div
a over JavaScript navigation
input/select/textarea over custom text controls
details/summary or established disclosure pattern over custom accordion
semantic table over div grid for real tabular data

Native controls provide built-in keyboard and accessibility behavior.
Use ARIA to fill semantic gaps, not to replace HTML casually.
