# ARIA Decision

Before ARIA:
1. Is there a native HTML element that already expresses this?
2. Is the chosen widget actually the ARIA pattern you think it is?
3. Can you implement the required keyboard/focus/state behavior?
4. Can you test it with assistive technology?

If native semantics are sufficient, prefer them.
