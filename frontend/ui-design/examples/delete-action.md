# Delete Action Example

For deleting one document from a table:
- keep action near the row
- secondary menu is acceptable if there are many row actions
- use explicit `Delete document` label
- confirm when deletion is difficult to undo
- show which document will be deleted
- disable/lock only the affected action while request is running
- show success or recoverable failure
