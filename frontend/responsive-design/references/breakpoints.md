# Breakpoints

A breakpoint is a response to a layout/interaction failure.

Examples:
- nav no longer fits → collapse nav
- action group wraps awkwardly → menu/stack
- form columns become cramped → stack
- table loses scanability → transform/scroll

Do not define breakpoints solely as "mobile/tablet/desktop".
