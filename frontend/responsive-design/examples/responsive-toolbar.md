# Responsive Toolbar

Desktop:
`Create | Import | Export | Filter | Refresh | More`

Mobile:
`Create | Filter | More`

The primary action remains visible.
Secondary actions move into a menu.
The menu is keyboard/touch accessible.
